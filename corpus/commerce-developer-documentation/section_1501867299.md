---
id: "section_1501867299"
type: "section"
title: "Managing Theme Assets"
branch: "commerce-developer-documentation"
category: "commerce"
breadcrumb: "Commerce > Commerce Developer Documentation > Develop Themes > Best Practices for Creating Themes > Managing Theme Assets"
parent: "section_1510849826"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1501867299.html"
anchors: ["bridgehead_1501868097", "svg_1", "svg_1Layer_5", "svg_1Layer_1", "svg_1Node", "svg_1Bendy_Thin", "svg_1Swoopy_3", "svg_2", "svg_2Node", "svg_2Bendy_Thin", "svg_2Swoopy_3", "section_1501877234", "bridgehead_1502058240", "bridgehead_1502120589", "bridgehead_1502120635", "bridgehead_1502127772", "bridgehead_1502127777", "bridgehead_1502127785", "bridgehead_1502127789", "bridgehead_1502127793", "bridgehead_1502127799", "bridgehead_1502127803", "bridgehead_1502127806", "bridgehead_1502127814", "section_1502129020", "bridgehead_1502129055", "bridgehead_1502130354", "bridgehead_1502130361", "bridgehead_1502130482", "bridgehead_1502130492", "bridgehead_1502130502"]
sha256: "cf9669c4b285c894c687a8c9954a3b9ac8647f7b201a2a83a46b5b2b1f481b95"
---

An asset is an image or font that is not managed by a NetSuite account but still is part of a theme or extension. An example of an asset is a carousel image, an icon, or a new font. This can be either a pre-existing asset or one that you introduce as part of a new theme.

When you run the `gulp theme:fetch` command:

-   Assets for the active theme download to your Workspace/<THEME\_DIRECTORY>/assets folder. This is the location where you manage all of your assets (new and pre-existing).
    
-   Assets for any active extensions download to your Workspace/Extras/Extensions/<EXTENSION\_DIRECTORY>/assets folder. Do not move, delete, edit, or add files in this location.
    

When you activate your theme, all assets are placed in specific locations in your NetSuite File Cabinet based on parameters you specify when you deployed your theme (vendor name, theme name, and theme version). Later, when you activate a newer version of the same theme, your assets are now located in a different location in your File Cabinet. Your code must adapt to the change in the path. If you use absolute paths, the asset links break.

Important:

Do not use absolute paths when referring to assets in your code customzations and overrides. Asset files are managed in different locations in NetSuite based on the theme's vendor, extension name, version, and so on. As a best practice, always use the HTML and Sass helpers when referencing assets to ensure that you maintain dynamic paths without unnecessary code changes.

To maintain dynamic paths to your assets, regardless of the vendor, theme, version, and so on, use the following helpers:

-   [HTML Helpers](#section_1501877234)
    
-   [Sass Helpers](#section_1502129020)
    

## Example: {#bridgehead_1501868097}

Using the theme developer tools, you deploy a new theme (**MyTheme1**, Version: **1.0.0**). You then activate that theme for a specific site and domain. The compiler places all assets in your NetSuite File Cabinet. The exact location is specific to the SSP application, vendor, extension, and version that you specified when you deployed your theme.

This location might be similar to the following representation:

<a id="svg_1"></a>

                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       

Later, you decide to update your theme, giving it a new version, **2.0.0**. When you activate your latest version, the compiler places all assets into a new location:

<a id="svg_2"></a>

                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               

## HTML Helpers {#section_1501877234}

The following Handlebars.js helpers provide a means to maintain dynamic paths to your assets when customizing HTML templates.

-   [getExtensionAssetsPath(default\_value)](#bridgehead_1502058240)
    
-   [getExtensionAssetsPathWithDefault(config\_value, default\_value)](#bridgehead_1502127772)
    
-   [getThemeAssetsPath(default\_value)](#bridgehead_1502127789)
    
-   [getThemeAssetsPathWithDefault(config\_value, default\_value)](#bridgehead_1502127803)
    

## getExtensionAssetsPath(default\_value) {#bridgehead_1502058240}

Use this HTML template helper:

-   To access pre-existing assets included with an active extension
    
-   In your extension overrides (templates)
    

Note:

The `default_value` argument is the relative path to the asset.

### Example Syntax in a Template File: {#bridgehead_1502120589}

The following is an example of how to use this helper in an HTML template:

              `<img src="https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/%7B%7B%20getExtensionAssetsPath%20'img/logo.png'%20%7D%7D">` 
            

### Example Result: {#bridgehead_1502120635}

This helper returns the active extension's asset path (where <FULL\_PATH> is the SSP Application base path):

              `<FULL_PATH>/extensions/<VENDOR>/<EXTENSION>/<VERSION>/img/logo.png` 
            

## getExtensionAssetsPathWithDefault(config\_value, default\_value) {#bridgehead_1502127772}

Use this HTML template helper:

-   To access pre-existing assets included with an active extension
    
-   In your extension overrides (templates)
    
-   When you expect that the asset has a configured path in NetSuite, but want to provide a default path if the configuration does not exist.
    

Note:

The first argument (`config_value`) is the path as configured in NetSuite. In cases where the asset is not configured, you provide a fallback path (`default_value`) to be retrieved from the extension's assets.

### Example Syntax in a Template File: {#bridgehead_1502127777}

The following is an example of how to use this helper in an HTML template:

              `<img src="https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/%7B%7BgetExtensionAssetsPathWithDefault%20logo_path%20'img/logo.png'%20%7D%7D" >` 
            

### Example Result: {#bridgehead_1502127785}

This helper returns the active extension's asset path. If the first argument is defined, this helper returns the path as configured in NetSuite:

              `<MY_PATH>/img/other_logo.png` 
            

If the first argument is undefined, this helper uses the second argument to return the correct path of the active extension:

              `<FULL_PATH>/extensions/<VENDOR>/<EXTENSION>/<VERSION>/img/logo.png` 
            

## getThemeAssetsPath(default\_value) {#bridgehead_1502127789}

Use this HTML template helper:

-   To access new and pre-existing assets included in your theme directory
    
-   In your extension overrides (templates)
    
-   In your theme customizations (templates)
    

Using this helper, `default_value` is the relative path to the asset.

### Example Syntax in Template File: {#bridgehead_1502127793}

              `<img src="https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/%7B%7B%20getThemeAssetsPath%20'img/logo.png'%20%7D%7D" >` 
            

### Example Result: {#bridgehead_1502127799}

This helper returns the active theme's asset path (where <FULL\_PATH> is the SSP Application base path):

              `<FULL_PATH>/extensions/<VENDOR>/<THEME_NAME>/<VERSION>/img/logo.png` 
            

## getThemeAssetsPathWithDefault(config\_value, default\_value) {#bridgehead_1502127803}

Use this HTML template helper:

-   To access new and pre-existing assets included in your theme directory
    
-   In your extension overrides (templates)
    
-   In your theme customizations (templates)
    
-   When you expect that the asset has a configured path in NetSuite, but want to provide a default path if the configuration does not exist.
    

Using this helper, `config_value` is the path as configured in NetSuite. In cases where the asset is not configured, you provide a fallback path (`default_value`) to be retrieved from the extension's assets.

### Example Syntax in Template File: {#bridgehead_1502127806}

              `<img src="https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/%7B%7B%20getThemeAssetsPathWithDefault%20logo_path%20'img/logo.png'%20%7D%7D" >` 
            

### Example Result: {#bridgehead_1502127814}

This helper returns the active theme's asset path. If the first argument is defined, this helper returns the path as configured in NetSuite:

              `<MY_PATH>/img/other_logo.png` 
            

If the first argument is undefined, this helper uses the second argument to return the correct path of the active theme:

              `<FULL_PATH>/extensions/<VENDOR>/<THEME_NAME>/<VERSION>/img/logo.png` 
            

## Sass Helpers {#section_1502129020}

The following helpers provide a means to maintain dynamic paths to your assets when customizing Sass files:

-   [getExtensionAssetsPath($asset)](#bridgehead_1502129055)
    
-   [getThemeAssetsPath($asset)](#bridgehead_1502130482)
    

## getExtensionAssetsPath($asset) {#bridgehead_1502129055}

Use this Sass helper:

-   To access pre-existing assets included with an active extension
    
-   In your extension overrides (Sass)
    

### Example Syntax in a Sass File: {#bridgehead_1502130354}

              `body {    background-image: url(getExtensionAssetsPath('img/background-image.png')); }` 
            

### Example Result: {#bridgehead_1502130361}

This helper returns the active extension's asset path.

              `<FULL_PATH>/extensions/<VENDOR>/<EXTENSION>/<VERSION>/img/logo.png` 
            

## getThemeAssetsPath($asset) {#bridgehead_1502130482}

Use this Sass helper:

-   To access new and pre-existing assets included in your theme directory
    
-   In your extension overrides (Sass)
    
-   In your theme customizations (Sass)
    

### Example Syntax in a Sass File: {#bridgehead_1502130492}

              `body {     background-image: url(getThemeAssetsPath('font-awesome')); }` 
            

### Example Result: {#bridgehead_1502130502}

This helper returns the active theme's asset path.

              `<FULL_PATH>/extensions/<VENDOR>/<THEME_NAME>/<VERSION>/font-awesome` 
            

### Related Topics

-   [Best Practices for Creating Themes](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1510849826.html)
-   [General Best Practices for Themes](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1503696962.html)
-   [HTML Best Practices for Themes](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1500579310.html)
-   [Sass Best Practices for Themes](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1500579362.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)

window.addEventListener("load", function() { svgPanZoom('#svg\_1', { zoomEnabled: true, controlIconsEnabled: true }); svgPanZoom('#svg\_2', { zoomEnabled: true, controlIconsEnabled: true }); },false); window.addEventListener("resize", function(){ svgPanZoom('#svg\_1').resize(); svgPanZoom('#svg\_1').fit(); svgPanZoom('#svg\_1').center(); svgPanZoom('#svg\_2').resize(); svgPanZoom('#svg\_2').fit(); svgPanZoom('#svg\_2').center(); },false);
