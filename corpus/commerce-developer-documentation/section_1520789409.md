---
id: "section_1520789409"
type: "section"
title: "Implement Asynchronous and Synchronous Methods"
branch: "commerce-developer-documentation"
category: "commerce"
breadcrumb: "Commerce > Commerce Developer Documentation > Develop Extensions > Do More With Extensions > Implement Asynchronous and Synchronous Methods"
parent: "article_161859012862"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1520789409.html"
anchors: ["subsect_1520990206"]
sha256: "147ba2215a9a3fc217652dc9626867ed688e1557b36a49a86931c72790811e88"
---

The Extensibiity API handles tasks synchronously in the frontend and both synchronously and asynchronously in the backend (the backend includes two versions of each method). Asynchronous methods uses the jQuery Deferred Object API. This API provides an easy way to register callbacks and monitor the success or failure of data transactions.

All asynchronous methods return a promise through the `deferred` object. A promise represents the eventual result of any asynchronous process. Any method that returns a `deferred` object is an asynchronous method.

The extensibility API implements the `deferred` object using the `jQuery.deferred()` method. Whenever an asynchronous operation runs, it returns a deferred promise.

This utility object allows you to relay the success or failure state of the operation and continue with your desired flow of execution.

Returning the `Deferred` object gives you the flexibility to use the promises returned by the default `Deferred` object of the Extensibility API or the `jQuery.deferred()` method. For more information about these promises, see [https://api.jquery.com/category/deferred-object](https://api.jquery.com/category/deferred-object) and [www.promisejs.org](http://www.promisejs.org).

Note:

The implementation of `deferred` is similar to standard promises, except that it uses `.fail()` instead of `.catch()`.

## jQuery.deferred() {#subsect_1520990206}

To use the promises returned by the jQuery library, you must instantiate `jQuery.deferred()` in your extension code.

For example, you might use `jQuery.Deferred().resolve()` to return a resolved promise in your code, especially in cases when you cannot use Backbone to implement asynchronous behavior.

The following example shows the instantiation in a JavaScript (frontend) or SuiteScript (backend) file:

            `define('MyExtension', ['jQuery'], function(jQuery) {   var promise = jQuery.deferred(); })` 
          

### Related Topics

-   [Do More With Extensions](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_161859012862.html)
-   [Access Configuration Properties](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1520999017.html)
-   [Localize Text in an Extension](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1516657590.html)
-   [Extensions for Site Management Tools (SMT)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_162980723798.html)
-   [Modal Dialogs in SCIS](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1545236555.html)
-   [Make a View Available Anywhere in Your Templates](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_0602032721.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
