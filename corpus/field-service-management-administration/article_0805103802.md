---
id: "article_0805103802"
type: "article"
title: "Date, Time, and Datetime Values for FSM Mobile Elements"
branch: "field-service-management-administration"
category: "field-service-management"
breadcrumb: "Field Service Management > Field Service Management Administration > Field Service Management Configuration > Field Service Configuration Reference > Task and Project Task Data in FSM Mobile > Date, Time, and Datetime Values for FSM Mobile Elements"
parent: "article_0730090514"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_0805103802.html"
anchors: ["subsect_0730110902", "subsect_0730111010", "subsect_0730124004"]
sha256: "cbf9891c9fb3793879288e437cbe10d26de83856e87d22fba5627fd339082229"
---

The value, min, and max properties must resolve to the format required by the target element. This applies to fixed values, template tags, expressions, and formula results. FSM ignores a configured property whose result does not match the required format.

| Element Type | Required Format | Example |
| --- | --- | --- |
| date | YYYY-MM-DD | 2026-07-14 |
| timeofday | HH:mm:ss | 09:30:00 |
| datetime | YYYY-MM-DD HH:mm:ss | 2026-07-14 09:30:00 |

The required stored value is independent of the localized format displayed in the mobile user interface.

This topic also includes:

-   [Default Date, Time, and Datetime Values in New FSM Mobile Records](#subsect_0730110902)
    
-   [Leaving FSM Mobile Date, Time, and Datetime Elements Blank](#subsect_0730111010)
    
-   [Using Date and Time Helpers in FSM Mobile Expressions](#subsect_0730124004)
    

## Default Date, Time, and Datetime Values in New FSM Mobile Records {#subsect_0730110902}

When `value` is not configured:

| Element Type | Default Behavior |
| --- | --- |
| date | Populates with the current date. |
| timeofday | Has no value. |
| datetime | Populates with the current date and time. |

## Leaving FSM Mobile Date, Time, and Datetime Elements Blank {#subsect_0730111010}

Use the JSON `null` value to explicitly leave an element blank:

            `"value": null` 
          

This is the recommended form because it clearly indicates that the element has no initial value. FSM also treats "" and "null" as blank, but do not use these forms in new configuration: the first is an empty string and the second is text rather than the JSON null value.

When a value constraint uses an expression or template , every non-null result path must return the exact format required by the target element. Return null when the element should have no value.

            `"value": "useCurrentDate.value ? now().date : null"` 
          

## Using Date and Time Helpers in FSM Mobile Expressions {#subsect_0730124004}

Use `now()` in a mobile expression when a property needs the current date, time, or datetime.

| Expression | Result Format |
| --- | --- |
| now().date | YYYY-MM-DD |
| now().time | HH:mm:ss |
| now().datetime | YYYY-MM-DD HH:mm:ss |

Use `format(...)` to return the date, time, or datetime portion of a field value in the format required by the target property.

| Expression | Result Format |
| --- | --- |
| format(field.value).date | YYYY-MM-DD |
| format(field.value).time | HH:mm:ss |
| format(field.value).datetime | YYYY-MM-DD HH:mm:ss |

Use the helper member that matches the target element type. The helpers can be used in dynamic mobile resource block properties, including `value`, `min`, and `max`.

### Related Topics

-   [Task and Project Task Data in FSM Mobile](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_0730090514.html)
-   [FSM Mobile Event Map Field Components](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_0805103342.html)
-   [FSM Mobile Template Tags](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_0805103619.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
