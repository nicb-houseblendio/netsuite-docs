---
id: "article_2164733370"
type: "article"
title: "createEvent()"
branch: "netsuite-cpq-configurator"
category: "netsuite-cpq"
breadcrumb: "NetSuite CPQ > NetSuite CPQ Configurator > Working with Product Building Blocks > List of Functions > createEvent()"
parent: "section_37154604037"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_2164733370.html"
anchors: ["subsect_73172450915", "subsect_0427100351", "subsect_40172459255", "subsect_21172511700", "subsect_8172603231", "subsect_96172610102", "subsect_92172617217", "subsect_98172623961"]
sha256: "e6dcab8da3fe9bcf2331df6eecc72a39120f2061ddc81002953c8fc5d96d1ee9"
---

The `createEvent()` function (client-side) creates a custom event that helps different actions coordinate asynchronous steps without having to call each other directly. You can create the event in one action, attach callbacks in the same or another action, and complete it later by calling `resolve()` or `reject()`. This function is useful when your implementation flow depends on timing-like waiting for a user interface element, an external script, or another action to finish.

The `createEvent()` function works together with `getEvent()` to retrieve the event and trigger it when the condition is met. Use `getEvents()` when you need to work with multiple events at the same time.

## Syntax {#subsect_73172450915}

Use this syntax for the `createEvent()` function:

            `createEvent('eventName');` 
          

## Return Value {#subsect_0427100351}

The `createEvent()` function returns an event object. You can use this object to register callbacks and control when the event is completed. The following table outlines the methods supported by the returned event object.

| Method | Description |
| --- | --- |
| `done(function(data) { // Success callback });` | Registers a success callback. |
| `fail(function(data) { // Failure callback });` | Registers a failure callback. |
| `then(function(data) { // Always callback });` | Registers a callback that runs after either success or failure. Note: `data` can be an object, a string, or a number. |
| `resolve(data);` | Marks the event as successful and passes data to callbacks. Note: `data` can be an object, a string, or a number. |
| `reject(data);` | Marks the event as failed and passes data to callbacks. Note: `data` can be an object, a string, or a number. |
| `resolveSpecial(object);` | Resolves the event when a custom condition is met. |

For the `resolveSpecial()` method, use these properties for the object taken as a parameter:

-   `func` - Runs this function until it returns `true`.
    
-   `time` - Sets the total wait duration in milliseconds. The default value is `5000`.
    
-   `delay` - Sets the interval between checks in milliseconds. The default value is `100`.
    
-   `data` - Passes this information to the callbacks when the event resolves.
    

## Parameters {#subsect_40172459255}

The `createEvent()` function accepts the event name in string format as a required parameter.

## Examples {#subsect_21172511700}

The following examples show how to use the `createEvent()` function.

### Creating an Event {#subsect_8172603231}

This example creates an event called `connect`.

              `createEvent('connect');` 
            

### Retrieving an Existing Event Object {#subsect_96172610102}

This example retrieves a previously created event.

              `var event = getEvent('connect');` 
            

### Creating and Resolving an Event With Data {#subsect_92172617217}

For example, you can start by creating an event.

              `createEvent('myEvent');` 
            

In another action or later in the code, you can retrieve the event and register success and failure callback.

              `var event = getEvent('myEvent'); event.done(function(data) {     console.log('Event resolved with:', data); }); event.fail(function(err) {     console.log('Event rejected with:', err); });` 
            

Lastly, you can resolve the event when appropriate.

              `event.resolve({ success: true });` 
            

### Resolving an Event When a Condition Is Met {#subsect_98172623961}

This example uses `resolveSpecial()` to verify a condition. The event is resolved when the function returns `true` or stops when the timeout is reached.

              `createEvent('draw');  var event = getEvent('draw');  event.done(function(data) {     console.log('Ready:', data); });  event.resolveSpecial({     func: function() {         return jQuery('#canvas01')[0];     },     time: 1000,     delay: 10,     data: 'canvas rendered' });` 
            

### Related Topics

-   [List of Functions](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_37154604037.html)
-   [deleteEvent()](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_5172520553.html)
-   [getEvent()](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_180927184.html)
-   [getEvents()](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_3181141105.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
