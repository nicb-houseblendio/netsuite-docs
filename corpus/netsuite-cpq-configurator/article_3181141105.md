---
id: "article_3181141105"
type: "article"
title: "getEvents()"
branch: "netsuite-cpq-configurator"
category: "netsuite-cpq"
breadcrumb: "NetSuite CPQ > NetSuite CPQ Configurator > Working with Product Building Blocks > List of Functions > getEvents()"
parent: "section_37154604037"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_3181141105.html"
anchors: ["subsect_10100312929", "subsect_27155239044", "subsect_83100319043", "subsect_57100327301", "subsect_59100335969"]
sha256: "70742ce710052d7c136da43f0a240f7dd7df963ba3846888ea636db6c129609a"
---

The `getEvents()` function (client-side) handles a group of synchronous events together to retrieve, monitor, and act on several events at the same time.

## Syntax {#subsect_10100312929}

Use this syntax for the `getEvents()` function:

            `var events = getEvents(['eventName1','eventName2', 'eventNamen']);` 
          

## Return Value {#subsect_27155239044}

The `getEvents()` function returns an object representing the specified events. This object includes the `name` property, which corresponds to the event names provided to the function. The object also exposes several methods to handle event resolution, failure, and listeners.

| Method | Description |
| --- | --- |
| `done(callback)` | Invokes the callback when any or all listed events resolve. The callback is attached to each event. |
| `fail(callback)` | Attaches the callback as a failure handler to all underlying events. |
| `reject(data)` | Invokes `reject(data)` on each event's deferred object, marking them as failed. |
| `resolve(data)` | Invokes `resolve(data)` on each event's deferred object, marking them as successful. |
| `resolveSpecial(options)` | If the verified condition returns `true`, this method resolves the events and applies the options to each event in the group. |
| `then(callback)` | Registers the callback to run after all events are resolved or rejected. |

## Parameters {#subsect_83100319043}

The `getEvents()` function accepts an array of strings representing the event names as a required parameter.

## Examples {#subsect_57100327301}

The following examples show how to use the `getEvents()` function.

### Attaching a Listener to All Events in a Group {#subsect_59100335969}

This example groups multiple events and runs a function when the specified events finish. The callback runs after all relevant events are resolved.

              `var events = getEvents(['floor.ready', 'canvas.ready']); events.then(function(data){     console.log('Floor and canvas are ready.'); });` 
            

### Related Topics

-   [List of Functions](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_37154604037.html)
-   [createEvent()](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_2164733370.html)
-   [deleteEvent()](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_5172520553.html)
-   [getEvent()](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_180927184.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
