---
id: "article_6192854478"
type: "article"
title: "priceMatrix()"
branch: "netsuite-cpq-configurator"
category: "netsuite-cpq"
breadcrumb: "NetSuite CPQ > NetSuite CPQ Configurator > Working with Product Building Blocks > List of Functions > priceMatrix()"
parent: "section_37154604037"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_6192854478.html"
anchors: ["subsect_32110924093", "subsect_0305113826", "subsect_72110931713", "subsect_38110939720", "subsect_31110949850"]
sha256: "6e410eb1453bf5c286492e71b51ab52fc11a73201386bd584e80b2e71c59182a"
---

The `priceMatrix()` function (client-side and server-side function) takes the item pricing information returned by `getItemPrice()` and returns an object wrapped in a method for retrieving prices. See also [getItemPrice()](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_182045044.html).

## Syntax {#subsect_32110924093}

Use this syntax for the `priceMatrix()` function:

            `prices = priceMatrix(priceData);` 
          

## Return Value {#subsect_0305113826}

The `priceMatrix()` function returns pricing information and the `getPrice()` method to work with it.

Use the following syntax for the `getPrice()` method:

            `prices.getPrice(priceLevel, qty);` 
          

Both parameters are optional. If the quantity is omitted, it defaults to 1. If the price level is omitted, it defaults to the lowest price level code. For example, if `getItemPrice()` returns the price levels 2 and 6, the methods returns the price for price level 2.

Note:

The `getPrice()` method supports unlimited price levels.

## Parameters {#subsect_72110931713}

The `priceMatrix()` function accepts the `priceData` parameter. This required parameter corresponds to the item pricing information returned by the `getItemPrice()` function.

## Examples {#subsect_38110939720}

The following examples show how to use the `priceMatrix()` function.

### Retrieving and Using an Item Price Matrix Asynchronously {#subsect_31110949850}

This example shows how to retrieve price details for a specific item using asynchronous code. First, the script retrieves the item pricing data using `getItemPrice()`. Then, it uses `priceMatrix()` to create an object to simplify price queries.

              `(async function() {     let data = await getItemPrice({         items: ['387']     });     let item = data.items[0];     console.log('DATA', item);     console.log('Pricing data', item.price);     let matrix = priceMatrix(item.price); // Creating a matrix wrapper     console.log('Price matrix', matrix);     console.log('Price (plevel=1, qty=10)', matrix.getPrice(1, 10)); })();` 
            

### Related Topics

-   [List of Functions](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_37154604037.html)
-   [getItemPrice()](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_182045044.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
