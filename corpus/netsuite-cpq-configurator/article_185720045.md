---
id: "article_185720045"
type: "article"
title: "loadConfig()"
branch: "netsuite-cpq-configurator"
category: "netsuite-cpq"
breadcrumb: "NetSuite CPQ > NetSuite CPQ Configurator > Working with Product Building Blocks > List of Functions > loadConfig()"
parent: "section_37154604037"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_185720045.html"
anchors: ["subsect_56181925428", "subsect_8181932531", "subsect_79181938584", "subsect_10181945438", "subsect_45181952753", "subsect_48182006268"]
sha256: "55616a6935f1e126abe64d86c9d4cef38d67a584c29e25727bc167c9edb8eeaf"
---

The `loadConfig()` function (server-side) loads configuration data using the configuration ID.

## Syntax {#subsect_56181925428}

Use this syntax for the `loadConfig()` function:

            `loadConfig(configId).done(callback);` 
          

## Return Value {#subsect_8181932531}

The `loadConfig()` function returns a promise that resolves to an object containing configuration data.

## Parameters {#subsect_79181938584}

Note:

The configuration ID is required.

The `loadConfig()` function accepts the ID of the configuration to load as a string or number parameter.

## Examples {#subsect_10181945438}

The examples show how to use the `loadConfig()` function.

### Loading Configuration Data with Then {#subsect_45181952753}

This example loads configuration data using the `.then()` method to handle the returned promise. The returned object is printed to the console.

              `loadConfig(1).then(function rdata(data) {     console.log(data); })` 
            

### Loading Configuration Data with Await {#subsect_48182006268}

This example shows how to use `async` and `await` syntax to work with promises.

              `let config = await loadConfig(1); console.log(config);` 
            

The script waits for `loadConfig()` to finish loading the configuration data and then logs the configuration object to the console. The returned object may look like this:

              `{     "status": "ok",     "qas": {         "productid": "1",         "productname": "Mountain bike",         // ...     } }` 
            

### Related Topics

-   [List of Functions](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_37154604037.html)
-   [submitConfig()](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_5102139129.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
