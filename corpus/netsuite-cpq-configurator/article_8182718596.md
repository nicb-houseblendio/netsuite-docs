---
id: "article_8182718596"
type: "article"
title: "getOrderField()"
branch: "netsuite-cpq-configurator"
category: "netsuite-cpq"
breadcrumb: "NetSuite CPQ > NetSuite CPQ Configurator > Working with Product Building Blocks > List of Functions > getOrderField()"
parent: "section_37154604037"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_8182718596.html"
anchors: ["subsect_28181131999", "subsect_0306094558", "subsect_73181139796", "subsect_32181147771", "subsect_18181206285"]
sha256: "69c04d541c6dfa7dc1522a513ad1623a6bb35f579ca9cf5a82d52efcac66aaf1"
---

The `getOrderField()` function (client-side) retrieves the value of body fields belonging to the transaction page from which users open the product. The transaction must be available for NetSuite CPQ Configurator. For more information about compatible transactions, see [Selecting Eligible Transactions](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_9124208079.html#subsect_0515013646).

## Syntax {#subsect_28181131999}

Use these syntax formats for the `getOrderField()` function.

-   To retrieve a single field:
    
                    `getOrderField('fieldId');` 
                  
    
-   To retrieve multiple fields:
    
                    `getOrderField(['fieldId1', 'fieldId2', 'fieldIdn']);` 
                  
    

## Return Value {#subsect_0306094558}

The `getOrderField()` function returns either a string with the field internal ID or an array of strings with the field internal IDs.

## Parameters {#subsect_73181139796}

As a required parameter, the `getOrderField()` function accepts either a string with the field ID or an array of strings with the field IDs you want to retrieve.

Results for dropdown lists, multi-select fields, and image fields return the internal ID of the selected option or image. To get the selected option label or the image relative URL in the File Cabinet, add the `:text` suffix to the field ID. For example, `'location:text'` will give you the location's name, such as `San Francisco`.

To retrieve the field label, add the `:label` suffix to the field ID. For example, `'location:label'` returns **Location**.

## Examples {#subsect_32181147771}

The following examples show how to use the `getOrderField()` function.

### Retrieving the Internal ID or the Label of a Transaction Field {#subsect_18181206285}

This example obtains the internal ID of the **Customer** field (field ID: entity).

              `getOrderField('entity');` 
            

See the output:

              `1387` 
            

Using the `:label` suffix, you can retrieve the field label.

              `getOrderField('entity:label');` 
            

See the output:

              `Customer` 
            

### Related Topics

-   [List of Functions](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_37154604037.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
