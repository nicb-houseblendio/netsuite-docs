---
id: "article_4094548260"
type: "article"
title: "runAction()"
branch: "netsuite-cpq-configurator"
category: "netsuite-cpq"
breadcrumb: "NetSuite CPQ > NetSuite CPQ Configurator > Working with Product Building Blocks > List of Functions > runAction()"
parent: "section_37154604037"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_4094548260.html"
anchors: ["subsect_18094102548", "subsect_10094109526", "subsect_78094116790", "subsect_41094125217", "subsect_95094134005"]
sha256: "c3732a5aed9f76d3568fdf791bdf64c024e914f34b6dce71cb2982c048d1aeb6"
---

The `runAction()` function (client-side) calls a server-side or client-side action.

## Syntax {#subsect_18094102548}

Use these syntax formats for the `runAction()` function:

-   To pass an object parameter:
    
                    `runAction({     code: 'ACTION_CODE',     async: true | false }).done(callback);` 
                  
    
-   To pass the server-side action code as string parameter (only works in asynchronous mode):
    
                    `runAction('ACTION_CODE').done(callback);` 
                  
    

## Return Value {#subsect_10094109526}

The `runAction()` function returns a promise that resolves to:

-   An object for server-side actions.
    
-   `undefined` for client-side actions.
    

## Parameters {#subsect_78094116790}

Note:

The action code is required.

When passing an object to the `runAction()` function, the object accepts the following properties:

-   `code` - Specifies the code of the action to call as a string. You can find the code in the **Code** field on the action record.
    
-   `async` - Determines whether the action call is synchronous or asynchronous. This property is `true` by default.
    

## Examples {#subsect_41094125217}

The following examples show how to use the `runAction()` function.

### Calling a Server-Side Action {#subsect_95094134005}

In this example, `runAction()` calls a server-side action in a loop until all records are created.

              `// Client-side action (async function() {     scratchpad.output = [];     scratchpad.records = getValue('DATA', 'RECORDS'); // Array of records to be created     scratchpad.index = 0;     let count = scratchpad.records.length;     do {         await runAction('CREATE_RECORDS');     } while (scratchpad.index < count);     console.log('FINISH', scratchpad.output); })();` 
            

### Related Topics

-   [List of Functions](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_37154604037.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
