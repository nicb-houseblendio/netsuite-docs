---
id: "article_1192649884"
type: "article"
title: "processRecordCreations()"
branch: "netsuite-cpq-configurator"
category: "netsuite-cpq"
breadcrumb: "NetSuite CPQ > NetSuite CPQ Configurator > Working with Product Building Blocks > List of Functions > processRecordCreations()"
parent: "section_37154604037"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_1192649884.html"
anchors: ["subsect_91121644025", "subsect_42121649295", "subsect_5121653898", "subsect_5104456527", "subsect_22104505632", "subsect_55104512759"]
sha256: "dba0d5ba763cb248ed399d21188bb95e635f18fe7a5d8baa807b9a5b32177bc5"
---

The `processRecordCreations()` function (client-side) creates records required by the configuration using all active creation records. Active creation records have a rule that matches user's choices on the product interface. The new records are saved in your account.

You can also verify that records have been correctly created, and use the console to view how records would be created to troubleshoot any possible issue. For more information about creation records, see [Creating Records Required by the Configuration](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_2131623448.html).

## Syntax {#subsect_91121644025}

Use this syntax for the `processRecordCreations()` function:

            `processRecordCreations({     mode: 'create' | 'log' }).then(callback);   .catch(callback);` 
          

## Return Value {#subsect_42121649295}

The `processRecordCreations()` function returns a promise that resolves to an object containing the results of the record creation process.

## Parameters {#subsect_5121653898}

Note:

All properties are optional.

The `processRecordCreations()` function accepts an object as a parameter. The object includes the `mode` property (string), which specifies how active creation records are processed. This property can take the following values:

-   -   `create` - Creates records from active creation records during the configuration process. This is the default mode.
        
    -   `log` - Uses the console to log the record that would be created to troubleshoot any possible issue.
        

## Examples {#subsect_5104456527}

The following examples show how to use the `processRecordCreations()` function.

### Creating Records Required by the Configuration {#subsect_22104505632}

This example runs the creation of records in `create` mode, which creates the required records. When the promise resolves, the function logs the returned data to the console. Use this approach when the configuration must produce real records.

              `processRecordCreations({     mode: 'create' }).then(data => {     console.log('Done', data); });` 
            

### Logging Records That Would Be Created {#subsect_55104512759}

This example runs in `log` mode, so no records are created. Instead, the function prints details to the console showing which records would be created. Use this approach to verify that creation records will be processed correctly.

              `processRecordCreations({     mode: 'log' }).then(data => {     console.log('Done', data); });` 
            

### Related Topics

-   [List of Functions](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_37154604037.html)
-   [Creating Records Required by the Configuration](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_2131623448.html)
-   [processItemCreations()](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_6192511646.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
