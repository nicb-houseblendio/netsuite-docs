---
id: "article_0110042539"
type: "article"
title: "Create Multiple Sales Records Using a Scheduled Script"
branch: "suitescript"
category: "suitecloud-platform"
breadcrumb: "SuiteCloud Platform > SuiteScript > SuiteScript 2.x Code Samples Catalog > SuiteScript Samples by Script Type > Scheduled Script Samples > Create Multiple Sales Records Using a Scheduled Script"
parent: "article_1162421739"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_0110042539.html"
anchors: []
sha256: "5c1fc7a8199cf2c10c210a06a4925c995bafa3d79e4d755e1c6e92543eaca860"
---

The following sample shows how to use a scheduled script to create multiple sales records and log the record creation progress.

Note:

This script sample uses the `define` function, which is required for an entry point script (a script you attach to a script record and deploy). You must use the `require` function if you want to copy the script into the SuiteScript Debugger and test it. For more information, see [SuiteScript 2.1 Global Objects](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_4387171685.html).

          `/** * @NApiVersion 2.1 * @NScriptType ScheduledScript */  // This script creates multiple sales records and logs the record creation progress. define(['N/runtime', 'N/record'], function(runtime, record) {     return {         execute: function(context) {             var script = runtime.getCurrentScript();             for (x = 0; x < 500; x++) {                 var rec = record.create({                     type: record.Type.SALES_ORDER                 });                 script.percentComplete = (x * 100)/500;                 log.debug({                     title: 'New Sales Orders',                     details: 'Record creation progress: ' + script.percentComplete + '%'                 });             }          }     }; });` 
        

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
