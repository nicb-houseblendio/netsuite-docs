---
id: "section_0303050238"
type: "section"
title: "N/datasetLink Module Script Sample"
branch: "suitescript"
category: "suitecloud-platform"
breadcrumb: "SuiteCloud Platform > SuiteScript > SuiteScript 2.x API Reference > SuiteScript 2.1 Modules > N/datasetLink Module > N/datasetLink Module Script Sample"
parent: "article_162609851226"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_0303050238.html"
anchors: ["subsect_163103643917"]
sha256: "d9a75da7785e8fdc43c9147e716951da44d92b59a8e049064e7a59e31e359aaf"
---

The following script sample demonstrates how to use the features of the N/datasetLink module.

## Load Two Datasets and Link Them {#subsect_163103643917}

Note:

This sample script uses the require function so you can copy it into the SuiteScript Debugger and test it. You must use the define function in an entry point script (a script you attach to a script record and deploy). For more information, see [SuiteScript 2.1 Script Basics](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1504638761.html) and [SuiteScript 2.1 Script Types](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_4387172495.html).

This script sample loads two datasets and links them using a common column (posting period).

In this sample, the `id` parameters of [dataset.load(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_158990335776.html) represent the script IDs of plug-in scripts that create datasets using the Dataset Builder Plug-in. For more information, see [Workbook Builder Plug-in](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/part_160199602236.html). If you run this script sample in your account, make sure you use script IDs or saved dataset IDs that are valid in your account.

            `/**  * @NApiVersion 2.x  */ require(['N/datasetLink', 'N/dataset'], function(datasetLink, dataset) {     // Load the datasets to link     var budgetDataset = dataset.load({         id: 'customscript1772'     });     var salesDataset = dataset.load({         id: 'customscript1773'     });      // Create expressions for columns in each dataset     // The alias parameter value represents the alias set     // on the associated column in the dataset     var budgetMachinePeriodExp = budgetDataset.getExpressionFromColumn({         alias: 'budgetmachineperiod'     });     var postingPeriodExp = salesDataset.getExpressionFromColumn({         alias: 'postingperiod'     });      // Link the datasets     var myDatasetLink = datasetLink.create({         datasets: [budgetDataset, salesDataset],         expressions: [[budgetMachinePeriodExp, postingPeriodExp]],         id: 'myLinkedDatasetId'     }); });` 
          

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
