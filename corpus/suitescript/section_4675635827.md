---
id: "section_4675635827"
type: "section"
title: "Using SuiteScript 2.x to Retrieve a Sublist Subrecord"
branch: "suitescript"
category: "suitecloud-platform"
breadcrumb: "SuiteCloud Platform > SuiteScript > SuiteScript 2.x > SuiteScript 2.x Scripting Records and Subrecords > SuiteScript 2.x Scripting Subrecords > Scripting Subrecords that Occur on Sublist Lines > Using SuiteScript 2.x to Retrieve a Sublist Subrecord"
parent: "section_4675625582"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4675635827.html"
anchors: ["bridgehead_4691003403", "bridgehead_4690926340"]
sha256: "80faed337390ce9d2fd3af60b81a656854503a3bd1e0cae722a80047c440b349"
---

In some cases, you may want to retrieve data from a subrecord that occurs in a sublist field.

To retrieve a sublist subrecord, use the [N/record Module](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4267255811.html). You can use either dynamic or standard mode. For details, see the following sections:

-   [Retrieving a Sublist Subrecord in Dynamic Mode](#bridgehead_4691003403)
    
-   [Retrieving a Subrecord in Standard Mode](#bridgehead_4690926340)
    

## Retrieving a Sublist Subrecord in Dynamic Mode {#bridgehead_4691003403}

If your script uses dynamic mode, you can use the following procedure to retrieve a subrecord that occurs in a sublist field.

To learn about SuiteScript scripting modes, see [SuiteScript 2.x Standard and Dynamic Modes](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1524156901.html)

#### To retrieve a sublist subrecord in dynamic mode:

1.  Load the parent record.
    
2.  Select the line that contains the subrecord with the [Record.selectLine(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273169163.html).
    
3.  Get the subrecord using the [Record.getCurrentSublistSubrecord(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4296710967.html) method. This method takes two arguments:
    
    -   A sublistId.
        
    -   A fieldId, which identifies the field on the sublist that contains the subrecord. In the Records Browser, the field that holds the subrecord is always identified as a field of type summary.
        
    
    For example, let's say you're working with an employee or customer record. You could use an expression like the following to load an address subrecord from the entity's Address sublist:
    
                    `... var addressSubrecord = rec.getCurrentSublistSubrecord({     sublistId: 'addressbook',     fieldId: 'addressbookaddress' }); ...` 
                  
    
4.  To retrieve a value from the subrecord's body, use the [Record.getValue(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273154686.html). For example, you could use an expression like the following to retrieve a detail from the body of an address subrecord:
    
                    `... var cityValue = addressSubrecord.getValue({     fieldId: 'city' }); ...` 
                  
    
5.  If you want to retrieve a value from the subrecord's sublist, use the [Record.getSublistValue(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273166148.html) method.
    

Note:

For a full script example showing how to retrieve a sublist subrecord in dynamic mode, see [Retrieving an Order Schedule Subrecord Example](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4691002186.html).

## Retrieving a Subrecord in Standard Mode {#bridgehead_4690926340}

If your script uses standard mode, use the following procedure to retrieve a sublist subrecord.

To learn about SuiteScript scripting modes, see [SuiteScript 2.x Standard and Dynamic Modes](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1524156901.html)

#### To retrieve a subrecord in standard mode

1.  Load the parent record.
    
2.  Retrieve the subrecord with the [Record.getSublistSubrecord(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4296711506.html) method. The method requires three arguments:
    
    -   A sublistId.
        
    -   A fieldId, which identifies the field on the sublist that contains the subrecord. In the Records Browser, the field that holds the subrecord is always identified as a field of type `summary`.
        
    -   A line number
        
    
    For example, you could use an expression like the following to load an order schedule subrecord from a blanket purchase order record:
    
                    `... var orderScheduleSubrecord = rec.getSublistSubrecord({     sublistId: 'item',     fieldId: 'orderschedule',     line: 0 }); ...` 
                  
    
3.  To retrieve a value from the subrecord's body, use the [Record.getValue(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273154686.html) method.
    
4.  To retrieve a value from the subrecord's sublist, use the [Record.getSublistValue(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273166148.html) method.
    
    For example, the order schedule subrecord has a schedule sublist. If you wanted to retrieve the date value from the second line of the schedule sublist, you would use an expression like the following:
    
                    `... var dateValue = orderScheduleSubrecord.getSublistValue({     sublistId: 'schedule',     fieldId: 'trandate',     line: 1 }); ...` 
                  
    

Note:

For a full script example showing how to retrieve a sublist subrecord in standard mode, see [Retrieving an Address Subrecord Example](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4690199022.html).

### Related Topics

-   [Scripting Subrecords that Occur on Sublist Lines](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4675625582.html)
-   [Using SuiteScript 2.x to Create a Subrecord in a Sublist Field](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4686991741.html)
-   [Using SuiteScript 2.x to Edit a Subrecord that Occurs in a Sublist Field](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4675630276.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
