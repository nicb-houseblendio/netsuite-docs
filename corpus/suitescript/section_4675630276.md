---
id: "section_4675630276"
type: "section"
title: "Using SuiteScript 2.x to Edit a Subrecord that Occurs in a Sublist Field"
branch: "suitescript"
category: "suitecloud-platform"
breadcrumb: "SuiteCloud Platform > SuiteScript > SuiteScript 2.x > SuiteScript 2.x Scripting Records and Subrecords > SuiteScript 2.x Scripting Subrecords > Scripting Subrecords that Occur on Sublist Lines > Using SuiteScript 2.x to Edit a Subrecord that Occurs in a Sublist Field"
parent: "section_4675625582"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4675630276.html"
anchors: ["bridgehead_4688367722", "bridgehead_4688367824"]
sha256: "8e37ff398ab801e51c2a4d8d5c081d2583eded50411c6a459d61f1e5b8db3e4c"
---

Your script can edit a subrecord in a sublist field in some cases.

To edit a sublist subrecord, your script must use the [N/record Module](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4267255811.html). You can use either dynamic or standard mode. For details, see the following sections:

-   [Editing a Subrecord in Dynamic Mode](#bridgehead_4688367722)
    
-   [Editing a Subrecord in Standard Mode](#bridgehead_4688367824)
    

To learn about SuiteScript scripting modes, see [SuiteScript 2.x Standard and Dynamic Modes](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1524156901.html)

## Editing a Subrecord in Dynamic Mode {#bridgehead_4688367722}

If your script uses dynamic mode, you can use the following procedure to edit a subrecord that occurs in a sublist field.

#### To edit a subrecord in dynamic mode:

1.  Load the record.
    
2.  Use the [Record.selectLine(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273169163.html) method to identify the sublist and line that contain the subrecord that you want to update.
    
3.  Get the subrecord using the [Record.getCurrentSublistSubrecord(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4296710967.html) method. The method requires two arguments:
    
    -   A sublistId.
        
    -   A fieldId, which identifies the field on the sublist that contains the subrecord. In the Records Browser, the field that holds the subrecord is always identified as a field of type summary.
        
    
    For example, suppose you are working with an entity record, such as an employee or customer. You could use an expression like the following to load an address subrecord from the entity's Address sublist:
    
                    `... var addressSubrecord = rec.getCurrentSublistSubrecord({     sublistId: 'addressbook',     fieldId: 'addressbookaddress' }); ...` 
                  
    
4.  Update the subrecord's body fields using the [Record.setValue(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273155868.html) method as needed. For example, you could use an expression like the following to update a value on the address subrecord:
    
                    `... addressSubrecord.setValue({     fieldId: 'city',     value: 'St. Petersburg' }); ...` 
                  
    
    However, note that some subrecords do not have writable body fields.
    
5.  If the subrecord has a sublist you want to modify, follow these steps for each line you want to change:
    
    1.  Identify the line you want to change using the [Record.selectLine(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273169163.html) method.
        
    2.  For each value you want to change, use the [Record.setCurrentSublistValue(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273171484.html) method to identify the field and the new value.
        
    3.  Save your changes to the subrecord's sublist line using the [Record.commitLine(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273168899.html) method.
        
6.  Save the line that holds the subrecord with the [Record.commitLine(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273168899.html) method.
    
7.  Save the record with the [Record.save(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4267286323.html) method.
    

Note:

For a full script example that shows editing a sublist subrecord in dynamic mode, see [Updating an Order Schedule Sublist Subrecord Example](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4688683936.html).

## Editing a Subrecord in Standard Mode {#bridgehead_4688367824}

If your script uses standard mode, you can use the following procedure to edit a subrecord that occurs in a sublist field.

To learn about SuiteScript scripting modes, see [SuiteScript 2.x Standard and Dynamic Modes](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1524156901.html)

#### To edit a subrecord in standard mode:

1.  Load the record.
    
2.  Retrieve the subrecord using the [Record.getSublistSubrecord(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4296711506.html) method. This method takes three arguments:
    
    -   A sublistId.
        
    -   A fieldId, which identifies the field on the sublist that contains the subrecord. In the Records Browser, the field that holds the subrecord is always identified as a field of type summary.
        
    -   A line number, which identifies the sublist line that contains the subrecord you want to change.
        
    
    For example, you could use an expression like the following to load an inventory detail subrecord from an item sublist:
    
                    `... inventoryDetailSubrecord = rec.getSublistSubrecord({     sublistId: 'item',     fieldId: 'inventorydetail',     line: 0 }); ...` 
                  
    
3.  Use the [Record.setValue(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273155868.html) method to update the subrecord's body fields. Be aware that not all subrecords have writable body fields.
    
4.  If the subrecord has a sublist whose values you want to modify, use the [Record.setSublistValue(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273166777.html) method to update the appropriate value. The method requires four arguments:
    
    -   A sublistId.
        
    -   A fieldId, which identifies the field you want to change.
        
    -   A line number, which identifies the sublist line you want to change.
        
    -   The new value.
        
    
    For example, if you were updating an inventory detail subrecord, you could use the following expression to update the serial number on the first line of the inventory assignment sublist:
    
                    `... inventoryDetailSubrecord.setSublistValue({     sublistId: 'inventoryassignment',     fieldId: 'receiptinventorynumber',     line: 0,     value: '56789' }); ...` 
                  
    
5.  Save the record with the save() method.
    

Note:

For a full script example showing how to modify a subrecord in standard mode, see [Updating an Address Subrecord Example](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4688684179.html).

### Related Topics

-   [Scripting Subrecords that Occur on Sublist Lines](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4675625582.html)
-   [Using SuiteScript 2.x to Create a Subrecord in a Sublist Field](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4686991741.html)
-   [Using SuiteScript 2.x to Retrieve a Sublist Subrecord](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4675635827.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
