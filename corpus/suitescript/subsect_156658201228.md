---
id: "subsect_156658201228"
type: "subsect"
title: "Create and Submit an Asynchronous Search Task and Export the Results into a CSV File"
branch: "suitescript"
category: "suitecloud-platform"
breadcrumb: "SuiteCloud Platform > SuiteScript > SuiteScript 2.x API Reference > SuiteScript 2.1 Modules > N/task Module > N/task Module Script Samples > Create and Submit an Asynchronous Search Task and Export the Results into a CSV File"
parent: "section_0305023241"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/subsect_156658201228.html"
anchors: []
sha256: "c16f59f7e817466b911d053175fd70d688550d4dcb90d151e6f521b1876706cc"
---

The following sample creates an asynchronous search task to execute a saved search and export the results of the search into a CSV file stored in the File Cabinet. After the search task is submitted, the sample retrieves the task status using the task ID. Some of the values in this sample are placeholders. Before using this sample, replace all hard-coded values, such as IDs and file paths, with valid values from your NetSuite account. If you run a script with an invalid value, the system may throw an error.

Note:

This sample script uses the `require` function so that you can copy it into the SuiteScript Debugger and test it. You must use the `define` function in an entry point script (the script you attach to a script record and deploy). For more information, see [SuiteScript 2.1 Script Basics](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1504638761.html) and [SuiteScript 2.1 Script Types](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_4387172495.html).

Important:

This sample uses SuiteScript 2.1. For more information, see [SuiteScript 2.1](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_156042690639.html).

          `/**  * @NApiVersion 2.1  */ require(['N/task'], task => {     // Do one of the following:     //     // - Create a saved search and capture its ID. To do this, you can use     //   the following code snippet (replacing the type, id, filters, and     //   columns values as appropriate):     //     //   let mySearch = search.create({     //       type: search.Type.SALES_ORDER,     //       id: 'customsearch_my_search',     //       filters: [...],     //       columns: [...]     //   });     //   mySearch.save();     //   let savedSearchId = mySearch.searchId;     //     // - Use the ID of an existing saved search. This is the approach that     //   this script sample uses. Update the following statement with the     //   internal ID of the search you want to use.     const savedSearchId = 669;       // Create the search task     let myTask = task.create({         taskType: task.TaskType.SEARCH     });     myTask.savedSearchId = savedSearchId;       // Specify the ID of the file that search results will be exported into     //     // Update the following statement so it uses the internal ID of the file     // you want to use     myTask.fileId = 448;       // Submit the search task     let myTaskId = myTask.submit();       // Retrieve the status of the search task     let taskStatus = task.checkStatus({         taskId: myTaskId     });       // Optionally, add logic that executes when the task is complete     if (taskStatus.status === task.TaskStatus.COMPLETE) {         // Add any code that is appropriate. For example, if this script created         // a saved search, you may want to delete it.     } });` 
        

### Related Topics

-   [N/task Module Script Samples](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_0305023241.html)
-   [Create and Submit a Map/Reduce Script Task](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/subsect_156658130732.html)
-   [Create and Submit a Task with Dependent Scripts](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/subsect_156658237922.html)
-   [Submit a Record Action Task and Check Status](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/subsect_156658362398.html)
-   [SuiteScript 2.1 Modules](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_4220488571.html)
-   [SuiteScript 2.x](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_8161516336.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
