---
id: "subsect_1508957748"
type: "subsect"
title: "Context Objects in Entry Point and Custom Module Scripts"
branch: "suitescript"
category: "suitecloud-platform"
breadcrumb: "SuiteCloud Platform > SuiteScript > SuiteScript 2.x > SuiteScript 2.1 API Introduction > SuiteScript 2.1 Script Basics > Context Objects in Entry Point and Custom Module Scripts"
parent: "section_1504638761"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/subsect_1508957748.html"
anchors: ["subsect_1510275939", "subsect_1510276599"]
sha256: "47acfdecd67246b85a02cd91a4afa9f861e375b70e1a04f42552341d96702bdf"
---

Every SuiteScript 2.1 script uses an entry point, whether it is an entry point script or a custom module script. Each entry point should correspond to an object defined in the script, which is usually a function. When it does, it is called an entry point function.

Most of the time, an entry point function gets access to an object provided by the system. This object lets your script access data and take actions based on the context where the script is running, which is why it's often called a context object.

Depending on what your script needs to do, this object can be a key part of your script. You can name it whatever you like, but most examples here call it **context**.

For more information about script types and entry points, see the [SuiteScript 2.1 Script Types and Entry Points](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/bridgehead_1501543623.html#bridgehead_1501609217) section under [SuiteScript 2.1 Development Concepts](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/bridgehead_1501543623.html).

## Context Objects in Entry Point Scripts {#subsect_1510275939}

Every standard script type has entry points specific to that type. Most of these entry points can access a context object that gives you data or methods. The properties of this object depend on the entry point you're using. For details about the context object for each entry point, check the documentation for that entry point.

For an example of how context objects work in a user event script, see [SuiteScript 2.1 User Event Script Tutorial](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1510274245.html).

## Context Objects in Custom Module Scripts {#subsect_1510276599}

Similar to entry point scripts, a custom module script's entry point function can also get a context object. But in this case, the object isn't provided by NetSuite, but rather it comes from the SuiteScript that calls the module.

If you want a custom entry point function to get an object, the calling SuiteScript needs to be set up for that. The calling script has to create the object, set any needed properties, and pass it to the custom module script, which can then use it.

For an example, see [Custom Module Tutorial](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1510274256.html).

To see how these components show up in scripts, check out [SuiteScript 2.1 Anatomy of a Script](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1510274310.html) and [Custom Module Script Anatomy](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1510274299.html).

### Related Topics

-   [SuiteScript 2.1 Script Basics](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1504638761.html)
-   [Objects in Standard Method Calls](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/subsect_1508957722.html)
-   [Modular Architecture](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/subsect_1508957818.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
