---
id: "section_1510274245"
type: "section"
title: "SuiteScript 2.1 User Event Script Tutorial"
branch: "suitescript"
category: "suitecloud-platform"
breadcrumb: "SuiteCloud Platform > SuiteScript > SuiteScript 2.x > SuiteScript 2.1 Script Types > SuiteScript 2.1 User Event Script Type > SuiteScript 2.1 User Event Script Reference > SuiteScript 2.1 User Event Script Tutorial"
parent: "section_1512408780"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1510274245.html"
anchors: ["subsect_1510173054", "subsect_1510193853", "subsect_1510288963", "subsect_1510289083", "subsect_1510173455", "subsect_1510264078", "subsect_1510270019", "subsect_1510289400", "subsect_1510289424", "subsect_1510289452", "subsect_1510289470", "subsect_1510298090", "subsect_1510298109", "subsect_1512514138", "subsect_1510942063"]
sha256: "a58c344bc1a29dc43a45bd6270c58ad713fc4d386138fabd4179d11f3a2df7fd"
---

A user event script runs on the server and is triggered when you take action on a record. It can run logic in three situations:

-   After you start to open the record, but before it loads.
    
-   After you click Save to save the record.
    
-   After the record's submitted to the database.
    

This tutorial walks you through how to set up a basic user event script. Here's what you'll do:

-   [Sample Script Overview](#subsect_1510173054)
    
-   Step One: [Check Your Prerequisites](#subsect_1510193853)
    
-   Step Two: [Create the Script File](#subsect_1510173455)
    
-   Step Three: [Review the Script (Optional)](#subsect_1510264078)
    
-   Step Four: [Upload the Script File to NetSuite](#subsect_1510298090)
    
-   Step Five: [Create a Script Record and Script Deployment Record](#subsect_1510298109)
    
-   Step Six: [Test the Script](#subsect_1512514138)
    

Note:

Before you start, check out [SuiteScript 2.1 Script Basics](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1504638761.html) for an overview of key terms. For details about the structure of SuiteScript 2.1 entry point scripts, see [SuiteScript 2.1 Anatomy of a Script](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1510274310.html).

## Sample Script Overview {#subsect_1510173054}

The sample script is meant for the employee record. When you deploy it, here's what it does:

-   When you start creating a new employee record, the script disables the Notes field before the page loads.
    
-   After you fill out all the required info and click Save, the script adds a note saying no orientation date has been set yet.
    
-   After the record's submitted, the script creates a task to schedule the new employee's orientation and assigns it to their supervisor.
    

This script uses all three [SuiteScript 2.1 User Event Script Type](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4387799721.html) entry points: [beforeLoad(context)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4407991781.html), [beforeSubmit(context)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4407992070.html), and [afterSubmit(context)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4407992281.html).

## Check Your Prerequisites {#subsect_1510193853}

Before you start, make sure your system is set up correctly:

-   [Enable the Feature](#subsect_1510288963)
    
-   [Create an Employee Record](#subsect_1510289083)
    

### Enable the Feature {#subsect_1510288963}

You'll need to enable the Client and Server SuiteScript features in your account. For help, see [Enabling SuiteScript](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N2903520.html#subsect_14174037626).

### Create an Employee Record {#subsect_1510289083}

You'll also need an employee record you can use as the supervisor for the test employee.

To see your current employee records, go to _Lists > Employees_. If you don't have one you can use, create a new employee. For help, see [Adding an Employee](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_N894212.html).

## Create the Script File {#subsect_1510173455}

Before you go any further, create the entry point script file. Copy and paste the following code into your favorite text editor, then save it as createTask.js.

            `/**   *@NApiVersion 2.x  *@NScriptType UserEventScript  */  // Load two standard modules. define ( ['N/record', 'N/ui/serverWidget'] ,     // Add the callback function.     function(record, serverWidget) {         // In the beforeLoad function, disable the Notes field.         function myBeforeLoad (context) {             if (context.type !== context.UserEventType.CREATE)                    return;             var form = context.form;               var notesField = form.getField({                    id: 'comments'             });               notesField.updateDisplayType({                 displayType: serverWidget.FieldDisplayType.DISABLED             });           }                  // In the beforeSubmit function, add test to the Notes field.         function myBeforeSubmit(context) {             if (context.type !== context.UserEventType.CREATE)                 return;             var newEmployeeRecord = context.newRecord;             newEmployeeRecord.setValue({                 fieldId: 'comments',                  value: 'Orientation date TBD.'             });         }                  // In the afterSubmit function, begin creating a task record.         function myAfterSubmit(context) {             if (context.type !== context.UserEventType.CREATE)                 return;                 var newEmployeeRecord = context.newRecord;             var newEmployeeFirstName = newEmployeeRecord.getValue ({                 fieldId: 'firstname'             });             var newEmployeeLastName = newEmployeeRecord.getValue ({                 fieldId: 'lastname'             });             var newEmployeeSupervisor = newEmployeeRecord.getValue ({                 fieldId: 'supervisor'             });             if (newEmployeeSupervisor) {                 var newTask = record.create({                     type: record.Type.TASK,                     isDynamic: true                 });                  newTask.setValue({                     fieldId: 'title',                     value: 'Schedule orientation session for ' +                          newEmployeeFirstName + ' ' + newEmployeeLastName                                });                 newTask.setValue({                     fieldId: 'assigned',                     value: newEmployeeSupervisor                 });                 try {                     var newTaskId = newTask.save();                     log.debug({                             title: 'Task record created successfully',                          details: 'New task record ID:  ' + newTaskId                     });                  } catch (e) {                      log.error({                          title: e.name,                             details: e.message                      });                 }             }         }                  // Add the return statement that identifies the entry point funtions.         return {             beforeLoad: myBeforeLoad,             beforeSubmit: myBeforeSubmit,             afterSubmit: myAfterSubmit         };        });` 
          

## Review the Script (Optional) {#subsect_1510264078}

If you want to see the components of the script, refer to the following sections.

-   [JSDoc Tags](#subsect_1510270019)
    
-   [beforeLoad Function](#subsect_1510289400)
    
-   [beforeSubmit Function](#subsect_1510289424)
    
-   [afterSubmit Function](#subsect_1510289452)
    
-   [return Statement](#subsect_1510289470)
    

### JSDoc Tags {#subsect_1510270019}

The following image shows the JSDoc block for this script. To see what the numbered callouts mean, look at the table after the image.

![JSDoc block with API Version and Script Type tags for a 2.x user event script.](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/img/SuiteCloudCustomizationScriptingWebServices/SuiteScript/SuiteScript20/SuiteScript20/anatomy_JsDoc.png)

| Callout | Description |
| --- | --- |
| 1 and 3 | The @NApiVersion tag (set to 2.x) is required in all entry point scripts. Valid values are 2.0, 2.x, and 2.1 |
| 2 and 4 | The @NScriptType tag (set to UserEventScript) shows what script type you're using. It's not case sensitive, but using Pascal case makes it easier to read. |

### beforeLoad Function {#subsect_1510289400}

The next image shows the myBeforeLoad function. Because of how the script's [return Statement](#subsect_1510289470) is set up, this function runs when beforeLoad is called.

![Sample beforeLoad entry point function.](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/img/SuiteCloudCustomizationScriptingWebServices/SuiteScript/SuiteScript20/SuiteScript20/anatomy_EntryPoint_beforeLoad.png)

| Callout | Description |
| --- | --- |
| 1 | The `context` object is available when beforeLoad runs. For a list of its properties, see [beforeLoad(context)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4407991781.html). This function uses two: `type` and `form`. |
| 2 | This statement makes the rest of the function run only if you're creating a new record. It checks the `context` object's `type` property to see what action you're taking. For possible actions, see [context.UserEventType](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4407992596.html). |
| 3 | These statements disable the Notes field. They use the `context` object's `form` property to access the form, and the [N/ui/serverWidget Module](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4321345532.html) APIs to change field settings. |

### beforeSubmit Function {#subsect_1510289424}

The next image shows the myBeforeSubmit function. Because of the [return Statement](#subsect_1510289470), this runs when beforeSubmit is called.

![Sample beforeSubmit entry point function.](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/img/SuiteCloudCustomizationScriptingWebServices/SuiteScript/SuiteScript20/SuiteScript20/anatomy_EntryPoint_beforeSubmit.png)

| Callout | Description |
| --- | --- |
| 1 | The `context` object is available for beforeSubmit-see [beforeSubmit(context)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4407992070.html) for its properties. This function uses `type` and `newRecord`. Note: The `context` object's properties here are different from [beforeLoad Function](#subsect_1510289400). The beforeLoad function has a form property; beforeSubmit doesn't, so you won't have access to the form in beforeSubmit. |
| 2 | This statement makes the rest of the function run only if you're creating a new record, using the `context` object's `type` property. See [context.UserEventType](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4407992596.html) for possible values. |
| 3 | These statements add text to the Notes field by using the `context` object's `newRecord` property. The script uses [Record.setValue(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273155868.html) to add the text. |

### afterSubmit Function {#subsect_1510289452}

The next image shows the myAfterSubmit function. Because of the [return Statement](#subsect_1510289470), this runs when afterSubmit is called.

The first part of the function gets data from the employee record:

![Sample afterSubmit entry point function.](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/img/SuiteCloudCustomizationScriptingWebServices/SuiteScript/SuiteScript20/SuiteScript20/anatomy_EntryPoint_afterSubmit_a.png)

| Callout | Description |
| --- | --- |
| 1 | The `context` object is available for afterSubmit-see [afterSubmit(context)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4407992281.html) for a list of its properties. This function uses `type` and `newRecord`. |
| 2 | This statement makes the rest of the function run only if you're creating a new record, using the `context` object's `type` property. For actions, see [context.UserEventType](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4407992596.html). |
| 3 | These statements use `context.newRecord` to access the submitted record and [Record.getValue(options)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4273154686.html) to get the data. |

The rest of the function creates the task record:

![Sample code to create record, set values, and save the record.](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/img/SuiteCloudCustomizationScriptingWebServices/SuiteScript/SuiteScript20/SuiteScript20/anatomy_EntryPoint_afterSubmit_b.png)

| Callout | Description |
| --- | --- |
| 4 | There's a conditional statement-it checks if the Supervisor field has a value. If so, the logic inside the curly braces runs. |
| 5 | These statements use the [N/record Module](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4267255811.html) APIs to create the new task and set its values. |
| 6 | This code tries to save the new task record. If there are errors, they're logged in the script deployment record's execution log. |

### return Statement {#subsect_1510289470}

The next image shows the callback function's return statement.

![Return statement for all user event script entry points.](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/img/SuiteCloudCustomizationScriptingWebServices/SuiteScript/SuiteScript20/SuiteScript20/anatomy_EntryPoint_return.png)

| Callout | Description |
| --- | --- |
| 1 | Entry points. The callback function's return statement must use at least one entry point for the script type set by @NScriptType. See Callout 4 in [JSDoc Tags](#subsect_1510270019). This script uses all three user event script entry points. |
| 2 | Entry points functions. For each entry point, your script needs to define a function for it. |

## Upload the Script File to NetSuite {#subsect_1510298090}

After you've created your entry point script file, upload it to your NetSuite File Cabinet.

**To upload the script file:**

1.  In the NetSuite UI, go to _Documents > Files > SuiteScripts_.
    
2.  In the left pane, select the SuiteScripts folder and click **Add File**.
    
3.  Follow the prompts to locate the createTask.js file (created in Step Two) in your local environment and upload it.
    

Even after you upload the file, you can edit it right from the File Cabinet if needed. For details, see the help topic [Editing Files in the File Cabinet](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_N542738.html).

## Create a Script Record and Script Deployment Record {#subsect_1510298109}

Before an entry point script can run in your account, you'll need to create a script record for the file and a script deployment record.

**To create the script record and script deployment record:**

1.  Go to _Customization > Scripting > Scripts > New_.
    
2.  In the **Script File** dropdown list, select createTask.js.
    
    If you haven't uploaded the file yet, you can do it here-click the plus sign next to the dropdown. You might need to hover your cursor to the right to see the icon.
    
3.  After you have selected the script file and it is displayed in the dropdown list, click the **Create Script Record** button.
    
    If your file passes validation, the system adds a new script record and lists createTask.js on the **Scripts** subtab.
    
4.  Fill out the required body fields as follows:
    
    -   In the **Name** field, enter **Create Task Record User Event**.
        
    -   In the **ID** field, enter **\_ues\_create\_task\_record**.
        
5.  Click the **Deployments** subtab.
    
6.  Add a line to the sublist, as follows:
    
    -   Set the **Applies to** dropdown list to **Employee**.
        
    -   In the **ID** field, enter **\_ues\_create\_task\_record**.
        
    
    Leave the other fields as they are. The **Status** field is set to **Testing** by default, so the script won't run for other users. If you want everyone to use it later, change the status to **Released**.
    
7.  Click **Save**.
    
    The system will create the script and its deployment records.
    

## Test the Script {#subsect_1512514138}

Now that the script's deployed, let's make sure it works.

**To test the script:**

1.  Start creating a new employee record.
    
    Go to _Lists > Employees > Employees > New_.
    
2.  On the new employee form, check that the Notes field is disabled. If beforeLoad worked, the field should be dimmed and not editable.
    
3.  Fill in all required fields-these might vary based on your account settings, but you'll need at least:
    
    -   **Name** - Enter a first name of John and last name of Smith.
        
    -   **Subsidiary** - (OneWorld only) Choose an appropriate value from the dropdown list.
        
4.  To make sure afterSubmit works, add a value for **Supervisor** if you haven't done so already.
    
5.  Click **Save**. You'll see a success message, and the new record appears in View mode.
    
6.  Check that beforeSubmit worked: The Notes field should say **Orientation date TBD**.
    
    ![Resulting Notes field on Employee page.](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/img/SuiteCloudCustomizationScriptingWebServices/SuiteScript/SuiteScript20/SuiteScript20/tutorialNewEmployee.png)
7.  Check that afterSubmit worked:
    
    1.  Select Activities > Scheduling > Tasks.
        
    2.  Make sure your view includes tasks assigned to all users.
        
    3.  Check that a task was created and assigned to the supervisor you picked earlier.
        
    
    ![Resulting task created and assigned.](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/img/SuiteCloudCustomizationScriptingWebServices/SuiteScript/SuiteScript20/SuiteScript20/tutorialNewTask.png)

## Next Steps {#subsect_1510942063}

If you want to customize this entry point script to call a custom module, see [Custom Module Tutorial](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1510274256.html).

### Related Topics

-   [SuiteScript 2.1 User Event Script Reference](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1512408780.html)
-   [How User Event Scripts are Executed](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1512409310.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
