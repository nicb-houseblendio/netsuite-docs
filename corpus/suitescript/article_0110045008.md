---
id: "article_0110045008"
type: "article"
title: "Perform Field Sourcing Synchronously"
branch: "suitescript"
category: "suitecloud-platform"
breadcrumb: "SuiteCloud Platform > SuiteScript > SuiteScript 2.x Code Samples Catalog > SuiteScript Samples by Function > Basic Record Operation Samples > Perform Field Sourcing Synchronously"
parent: "article_9190152309"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_0110045008.html"
anchors: []
sha256: "b13cb63a60818248c38d9de994254f29de7858ce25dc2eee38a936dc50ad2cf1"
---

The following script sample shows how to use the `forceSyncSourcing` parameter.

This parameter can be used to alleviate a timing situation that may occur in some browsers when fields are sourced. For some browsers, some APIs are triggered without waiting for the field sourcing to complete. For example, if forceSyncSourcing is set to false when adding sublist lines, the lines aren't committed as expected. Setting the parameter to true, forces synchronous sourcing.

This sample shows using the forceSyncSourcing parameter in the setCurrentSublistValue method. The forceSyncSourcing parameter is also available in the setText, setValue, setCurrentSublistText, setMatrixHeaderValue, and setCurrentMatrixSublistValue methods.

Note:

This sample script uses the `require` function so that you can copy it into the SuiteScript Debugger and test it. You must use the `define` function in an entry point script (the script you attach to a script record and deploy). For more information, see [SuiteScript 2.1 Script Basics](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1504638761.html) and [SuiteScript 2.1 Script Types](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_4387172495.html).

          `/**  * @NApiVersion 2.1  * @NScriptType ClientScript  */  define(['N/currentRecord'], (currentRecord) => {     const pageInit = () => {         const rec = currentRecord.get();          rec.selectNewLine({             sublistId: 'item'         });          rec.setCurrentSublistValue({             sublistId: 'item',             fieldId: 'item',             value: 39,             forceSyncSourcing: true         });          rec.setCurrentSublistValue({             sublistId: 'item',             fieldId: 'quantity',             value: 1,             forceSyncSourcing: true         });          rec.commitLine({             sublistId: 'item'         });     };      return {         pageInit     }; });` 
        

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
