---
id: "article_0111025227"
type: "article"
title: "Concatenate API Secrets with Strings"
branch: "suitescript"
category: "suitecloud-platform"
breadcrumb: "SuiteCloud Platform > SuiteScript > SuiteScript 2.x Code Samples Catalog > SuiteScript Samples by Module > N/https Samples > Concatenate API Secrets with Strings"
parent: "section_157072652325"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_0111025227.html"
anchors: []
sha256: "5123d7465b4e0104b612018c3540a7e71e8c1022d094bcb4ad496d26d4d28c84"
---

The following sample shows how to concatenate a string value to use as an API secret. API Secrets are string values that cannot be concatenated directly. In some cases, a code-generated string (for example, date stamp, account ID, sequence numbers) needs to be merged with the API secret. To merge the values, the `N/https` module must be imported on the script file and use the `createSecureString()` API to initialize both secret API values and ordinary string.

Note:

This sample script uses the `require` function so that you can copy it into the SuiteScript Debugger and test it. You must use the `define` function in an entry point script (the script you attach to a script record and deploy). For more information, see [SuiteScript 2.1 Script Basics](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_1504638761.html) and [SuiteScript 2.1 Script Types](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_4387172495.html).

Important:

This sample uses SuiteScript 2.1. For more information, see [SuiteScript 2.1](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_156042690639.html).

          `/**  * @NApiVersion 2.1  */  // This script uses appendSecureString to concatenate strings to use as an API secret. require(['N/https', 'N/runtime'], (https, runtime) => {     function concatToCreateSecureString() {         let baseUrl = https.createSecureString({             input: 'www.someurl.com/add?apikey='          });         let apiKey = https.createSecureString({             input: '{CUSTSECRET_SOME_INTEGRATION}'         });         let url = baseUrl.appendSecureString({             secureString: apiKey         });     }     concatToCreateSecureString(); });` 
        

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
