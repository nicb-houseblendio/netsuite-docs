---
id: "section_157297763683"
type: "section"
title: "Validate Order on Entry"
branch: "suitecloud-customization-tutorials"
category: "suitecloud-platform"
breadcrumb: "SuiteCloud Platform > SuiteCloud Customization Tutorials > Validate Order on Entry"
parent: "article_159163143019"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157297763683.html"
anchors: ["subsect_163330381323", "subsect_163330346350", "subsect_163330348287", "subsect_163330351295", "subsect_163330382825", "subsect_163330383418", "rc_1994127498", "subsect_163330384149", "subsect_163330384827", "subsect_163330385373", "subsect_158835971126", "subsect_158628033311", "subsect_158836033141", "subsect_158628057047", "subsect_158621404562", "subsect_163330390914", "subsect_158621495705", "subsect_163330354397", "subsect_163330357024", "subsect_163330358661", "subsect_163330360216"]
sha256: "195b2366e3029fead4ca2da83c9ba24437fec0a0918f894ae6a34066beb24a94"
---

This use case shows you how to validate the number of items in an order. You do this to ensure the number of items corresponds to the number of cases that fit on a pallet. This validation ensures that you accept only orders that are fulfilled by full cases. This use case also shows you how to verify that the total weight of the order doesn't exceed the maximum allowed for a single order.

This project is available on the [SuiteCloud Project Repository on Oracle Samples GitHub](https://github.com/oracle-samples/netsuite-suitecloud-samples) and includes a complete project customization setup with custom objects, unit tests, package file configuration, and more.

## Customization Details {#subsect_163330381323}

The customization for this use case includes:

-   A custom field (Cases per Pallet) to store the number of cases of the item on one pallet
    
-   A custom field (Item Weight) to store the weight of a single item
    
-   A client script triggered on the validateLine and saveRecord entry points
    
-   A script parameter (Max Weight) to store the maximum weight for an order
    

Steps in this tutorial to complete the customization:

-   [Before You Begin](#subsect_163330346350)
    
-   [Step 1: Create the Custom Fields](#subsect_163330348287)
    
-   [Step 2: Write the Script](#subsect_163330351295)
    
-   [Step 3: Create the Script Record](#subsect_163330354397)
    
-   [Step 4: Deploy the Script](#subsect_163330357024)
    
-   [Step 5: Create and Set the Script Parameter](#subsect_163330358661)
    
-   [Step 6: Test the Solution](#subsect_163330360216)
    

## Before You Begin {#subsect_163330346350}

The following table lists features, permissions, and other requirements necessary for performing this tutorial and implementing the solution:

| **Required Features** | Ensure the following features are enabled in your account:
-   **Client SuiteScript** - Lets you apply client scripts to records and forms.
-   **File Cabinet** - Lets you store your script files in the NetSuite File Cabinet.

For more information, see [Enabling Features](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N232138.html). |
| --- | --- |
| **Required Permissions** | You need a role with access to the following:

-   **Scripts** - Edit access
-   **Script Deployments** - Edit access
-   **Transaction Line Fields** - Create access
-   **Sales Orders** - Edit access

For more information, see [NetSuite Permissions Overview](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_N325094.html) |
| **Other Requirements** | None |

## Step 1: Create the Custom Fields {#subsect_163330348287}

This customization uses two custom fields: **Cases per Pallet** stores the number of cases of an item that fit on one pallet, and **Item Weight** stores the weight of a single item. Both fields are custom transaction line fields added to the Item sublist on a sales order record.

#### To create the Cases per Pallet field:

1.  Go to _Customization > Lists, Records, & Fields > Transaction Line Fields > New_.
    
2.  Enter or select values for fields as listed in the following table:
    
    | Field | Value |
    | --- | --- |
    | Label | **Cases per Pallet** |
    | ID | **\_cases\_per\_pallet** NetSuite prefixes the ID with **custcol**. |
    | Type | **Integer Number** |
    | Store Value | **Checked** |
    | Applies To | **Sale Item** |
    
3.  Click **Save**.
    

#### To create the Item Weight field:

1.  Go to _Customization > Lists, Records, & Fields > Transaction Line Fields > New_.
    
2.  Enter or select values for fields as listed in the following table:
    
    | Field | Value |
    | --- | --- |
    | Label | **Item Weight** |
    | ID | \_item\_weight NetSuite prefixes the ID with **custcol**. |
    | Type | **Decimal Number** |
    | Store Value | **Checked** |
    | Applies To | **Sale Item** |
    
3.  Click **Save**.
    

For more information about creating custom entity fields, see the following help topics:

-   [Creating a Custom Field](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_N2829580.html)
    
-   [Creating Custom Transaction Line Fields](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_N2828307.html)
    

## Step 2: Write the Script {#subsect_163330351295}

This script validates that the number of items in an order corresponds to the number of cases that fit on a pallet. This validation ensures that you accept only orders that are fulfilled by full cases. The script also verifies that the total weight of the order doesn't exceed the maximum allowed for a single order.

### Script Summary {#subsect_163330382825}

The following table summarizes the script:

| Script: Validate Order |
| --- |
| Script Type | [SuiteScript 2.1 Client Script Type](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4387798404.html) |
| Modules Used | 
-   [N/record Module](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4267255811.html)
-   [N/runtime Module](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4296359529.html)
-   [N/currentRecord Module](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4625600928.html) - This module is available to all scripts as a provided context object. You don't need to explicitly load this module as a dependency in your define or require statement, however, you may if you want. This tutorial doesn't explicitly load this module.
-   [N/log Module](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4574548135.html) - This module is available to all scripts as a global object. You should explicitly load this module to avoid conflicts with other objects named 'log'.

 |
| Entry Points | 

-   [saveRecord(scriptContext)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4410693749.html)
-   [validateLine(scriptContext)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4410693302.html)

 |

For more information about script types and entry points, see [SuiteScript 2.1 Script Types](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_4387172495.html).

### The Complete Script {#subsect_163330383418}

This tutorial includes the complete script along with individual steps you can follow to build the script in logical sections. The complete script is provided below so you can copy and paste it into your text editor and save it as a .js file (for example, cs\_validateOrder.js).

If you would rather create your script step by step, follow the steps in [Build the Script](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157243821278.html#subsect_163275714617).

Note:

This tutorial script uses the `define` function, which is required for an entry point script (a script you attach to a script record and deploy). If you want to copy the script into the SuiteScript Debugger and test it, you must use the `require` function. For more information, see [SuiteScript Debugger](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N3014215.html).

### Note {#rc_1994127498}

This sample uses SuiteScript 2.1.

              ``/**  * @NApiVersion 2.1  * @NScriptType ClientScript  * @NModuleScope SameAccount  */  define(['N/runtime', 'N/log'], (, runtime, log) => {     function validateLine(scriptContext) {         const recSalesOrder = scriptContext.currentRecord;         if (scriptContext.sublistId === 'item') {             const casePerPallet = recSalesOrder.getCurrentSublistValue({                 sublistId: 'item',                 fieldId: 'custcol_cases_per_pallet'             });             const quantity = recSalesOrder.getCurrentSublistValue({                 sublistId: 'item',                 fieldId: 'quantity'             });             if (quantity % casePerPallet !== 0) {                 alert(`Please order in multiples of ${casePerPallet} Case/Pallet for this item.`);                 return false;             } else {                 return true;             }         }         return true;     }     function saveRecord(scriptContext) {         const scriptObj = runtime.getCurrentScript();         const weightParam = scriptObj.getParameter({             name: 'custscript_max_weight'         });         if (NSUtil.isEmpty(weightParam)) {             log.error({                 title: 'saveRecord',                 details: 'Max weight script parameter not defined'             });             return;         }         const recSalesOrder = scriptContext.currentRecord;         const lineCount = recSalesOrder.getLineCount({             sublistId: 'item'         });         let totalWeight = 0;         for (let i = 0; i < lineCount; i++) {             const itemWeight = recSalesOrder.getSublistValue({                 sublistId: 'item',                 fieldId: 'custcol_item_weight',                 line: i             });             const quantity = recSalesOrder.getSublistValue({                 sublistId: 'item',                 fieldId: 'quantity',                 line: i             });             const lineWeight = itemWeight * quantity;             totalWeight += lineWeight;         }         if (totalWeight > weightParam) {             alert(`The total weight of the order is ${totalWeight}. The max weight is ${weightParam}. Please adjust the order and submit a separate order if necessary.`);             return false;         }         return true;     }     const NSUtil = {};     NSUtil.isEmpty = function(stValue) {         return(             stValue === "" || stValue === null || stValue === undefined ||             (stValue.constructor === Array && stValue.length === 0) ||             (stValue.constructor === Object &&             (function(v){                 for (const k in v) return false;                 return true;             })(stValue))         );     }     return {         validateLine: validateLine,         saveRecord: saveRecord     }; });`` 
            

### Build the Script {#subsect_163330384149}

You can write the script using a step-by-step approach that includes the following:

-   [Start with required opening lines](#subsect_163330384827)
    
-   [Create the entry point functions](#subsect_163330385373)
    
-   [Get the current record](#subsect_158835971126)
    
-   [Validate the cases per pallet](#subsect_158628033311)
    
-   [Get the script parameter](#subsect_158836033141)
    
-   [Validate the total weight of the order](#subsect_158628057047)
    
-   [Create the isEmpty function](#subsect_158621404562)
    
-   [Create the return statement](#subsect_163330390914)
    
-   [Save your script file](#subsect_158621495705)
    

Note:

The code snippets included below don't include indentation. Refer to [The Complete Script](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157243821278.html#subsect_163275714025) for suggested formatting.

#### Start with required opening lines {#subsect_163330384827}

You need JSDoc comments and a `define` function at the top of the script file. The JSDoc comments indicate that it's a SuiteScript 2.x (2.0 or 2.1) client script. The script uses three SuiteScript modules specified in the `define` statement:

-   `N/runtime` - provides access to script parameters
    
-   `N/log` - lets you log execution details
    

Start a new script file in any text editor and add the following JSDoc comments and `define` function at the top:

Note:

This tutorial script uses the `define` function, which is required for an entry point script (a script you attach to a script record and deploy). If you want to copy the script into the SuiteScript Debugger and test it, you must use the `require` function. For more information, see [SuiteScript Debugger](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N3014215.html).

                `/**  * @NApiVersion 2.1  * @NScriptType ClientScript  * @NModuleScope SameAccount  */  define(['N/runtime', 'N/log'], (runtime, log) => { });` 
              

#### Create the entry point functions {#subsect_163330385373}

This script is triggered on the `validateLine` entry point when an item is added to a sales order, and on the `saveRecord` entry point when a sales order is saved.

Add the following function definitions at the top of the `define` function:

                `function validateLine(scriptContext) {     return true; } function saveRecord(scriptContext) { }` 
              

#### Get the current record {#subsect_158835971126}

This script operates on the current sales order record. You need to get the current sales order record.

Add the following code at the top of the `validateLine` function, above the `return true;` statement:

                `const recSalesOrder = scriptContext.currentRecord;` 
              

#### Validate the cases per pallet {#subsect_158628033311}

You need to validate the value entered in the **Cases Per Pallet** column for each item on the sales order record. The **Quantity** entered for each item must correspond to the number of cases allowed per pallet for that item.

Add the following code to the `validateLine` function, above the `return true;` statement:

                ``if (scriptContext.sublistId === 'item') {     const casePerPallet = recSalesOrder.getCurrentSublistValue({         sublistId: 'item',         fieldId: 'custcol_cases_per_pallet'     });     const quantity = recSalesOrder.getCurrentSublistValue({         sublistId: 'item',         fieldId: 'quantity'     });     if (quantity % casePerPallet !== 0) {         alert(`Please order in multiples of ${casePerPallet} Case/Pallet for this item.`);         return false;     } else {         return true;     } }`` 
              

#### Get the script parameter {#subsect_158836033141}

When the sales order record is saved, you need to get the value of the script parameter (custscript\_max\_weight, set in Step 5) that specifies the maximum weight for an order.

Add the following code at the top of the `saveRecord` function:

                `const scriptObj = runtime.getCurrentScript(); const weightParam = scriptObj.getParameter({     name: 'custscript_max_weight' }); if (NSUtil.isEmpty(weightParam)) {     log.error({         title: 'saveRecord',         details: 'Max weight script parameter not defined'     });     return; }` 
              

#### Validate the total weight of the order {#subsect_158628057047}

You also need to verify that the total weight of the order doesn't exceed the maximum weight allowed, as set by the script parameter (custcol\_item\_weight, set in Step 5).

Add the following code to the `saveRecord` function:

                ``const recSalesOrder = scriptContext.currentRecord; const lineCount = recSalesOrder.getLineCount({     sublistId: 'item' }); let totalWeight = 0; for (let i = 0; i < lineCount; i++) {     const itemWeight = recSalesOrder.getSublistValue({         sublistId: 'item',         fieldId: 'custcol_item_weight',         line: i     });     const quantity = recSalesOrder.getSublistValue({         sublistId: 'item',         fieldId: 'quantity',         line: i     });     const lineWeight = itemWeight * quantity;     totalWeight += lineWeight; } if (totalWeight > weightParam) {     alert(`The total weight of the order is ${totalWeight}. The max weight is ${weightParam}. Please adjust the order and submit a separate order if necessary.`);     return false; } return true;`` 
              

#### Create the isEmpty function {#subsect_158621404562}

This script uses a utility function to determine if the value of the script parameter is empty.

Add the following code after the end of the `saveRecord` function:

                `const NSUtil = {}; NSUtil.isEmpty = function(stValue) {     return(         stValue === "" || stValue === null || stValue === undefined ||         (stValue.constructor === Array && stValue.length === 0) ||         (stValue.constructor === Object &&         (function(v){             for (const k in v) return false;             return true;         })(stValue))     ); }` 
              

#### Create the return statement {#subsect_163330390914}

This script associates the `validateLine` function with the `validateLine` client script entry point, and the `saveRecord` function with the `saveRecord` client script entry point.

Add the following code immediately above the closing `});` in your script:

                `return {     validateLine: validateLine,     saveRecord: saveRecord };` 
              

#### Save your script file {#subsect_158621495705}

You need to save your script file so you can upload it to the NetSuite File Cabinet. Before you save your script file, you may want to adjust the indentation to improve the readability of the script. Refer to [The Complete Script](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157243821278.html#subsect_163275714025) for suggested formatting.

When you're happy with your script, save it as a .js file (for example, cs\_validateOrder.js).

## Step 3: Create the Script Record {#subsect_163330354397}

Now that you've completed the script, you can upload the file to the File Cabinet and create a script record for it.

For more information about creating script records, see [Creating a Script Record](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4489062315.html).

#### To create the script record:

1.  Upload your script to the NetSuite File Cabinet.
    
2.  Go to _Customization > Scripting > Scripts > New_.
    
3.  Select your script from the **Script File** list and click **Create Script Record**. The Script page is displayed.
    
4.  On the Script page, enter the following values:
    
    | Field | Value |
    | --- | --- |
    | Name | **Validate Order** |
    | ID | **\_cs\_validate\_order** NetSuite prefixes the ID with **customscript**. |
    | Description | This script validates the number of items in an order. The script ensures the number of items in the order corresponds to the number of cases that fit on a pallet. This validation ensures that you accept only orders that are fulfilled by full cases. It also verifies that the total weight doesn't exceed the maximum allowed for a single order. |
    
5.  Set any other fields on the script record as required.
    
6.  Click **Save and Deploy**. The Script Deployment page appears. Continue with [Step 4: Deploy the Script](#subsect_163330357024).
    

## Step 4: Deploy the Script {#subsect_163330357024}

After you create the script record for your script, you can create a script deployment record for it. A script deployment record determines how, when, and for whom the script runs.

For more information about script deployment records, see [Script Deployment](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_4486246754.html).

#### To deploy the script:

1.  Complete the steps in [Step 3: Create the Script Record](#subsect_163330354397).
    
2.  On the Script Deployment page, enter the following values:
    
    | Field | Value |
    | --- | --- |
    | Applies To | **Sales Order** |
    | ID | **\_cs\_validate\_order** NetSuite prefixes the ID with **customdeploy**. |
    | Status | **Testing** The Testing status lets the script owner test the script without affecting other users in the account. |
    | Log Level | **Debug** The Debug level writes all `log.debug` statements and errors to the Execution Log subtab of the script deployment record. |
    | Audience > Roles | Check **Select All** |
    
3.  Click **Save**.
    

## Step 5: Create and Set the Script Parameter {#subsect_163330358661}

This tutorial uses a script parameter to specify the maximum allowed weight for a sales order.

For more information about creating and setting script parameters, see [Creating Script Parameters (Custom Fields)](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/part_1542289448.html).

#### To create the script parameter:

1.  Go to _Customization > Scripting > Scripts_.
    
2.  Find your script in the list and click **Edit** next to the script name.
    
3.  On the Script page, click the **Parameters** subtab and click **New Parameter**. The Script Field page appears.
    
4.  On the Script Field page, enter the following values:
    
    | Script Parameter | Label | ID | Type | Description | Preference |
    | --- | --- | --- | --- | --- | --- |
    | Maximum Weight | **Maximum Weight** | **\_max\_weight** NetSuite prefixes the ID with **custscript**, so the script uses **custscript\_max\_weight**. | **Decimal Number** | The maximum allowed weight for a sales order. | Leave blank |
    
5.  To save the script parameter, click **Save**. The Script page appears.
    
6.  To save the script record with the updated script parameter, click **Save**.
    

After you create the script parameter, set its value on the script deployment record that you created in Step 4.

#### To set the script parameter:

1.  Go to _Customization > Scripts > Script Deployments_.
    
2.  Find your script deployment in the list and click **Edit** next to the script deployment ID.
    
3.  On the **Parameters** subtab, enter **25** for the **Maximum Weight** script parameter.
    
4.  Click **Save**.
    

## Step 6: Test the Solution {#subsect_163330360216}

After you create the script record and deploy your script, you can test your solution by creating a sales order and adding items.

#### To test your solution:

1.  Go to _Transactions > Sales > Enter Sales Orders_ to create a new sales order.
    
2.  Fill in all required fields on the sales order with any values you want. You can use the Standard Sales Order form.
    
3.  On the Items subtab, add any item. Set the **Quantity** to 17, the **Cases Per Pallet** to 10, and the **Item Weight** to 5.
    
4.  Click **Add** to add the item. Verify that because the **Quantity** of the item (17) is not a value that corresponds to the **Cases Per Pallet** (10), an error occurs: 'Please order in multiples of 10 Cases/Pallet for this item' (which is text set in the script).
    
5.  Close the error message and change the **Quantity** value to 10 and then click **Add** . Verify that the item was successfully added. You now have an order of 10 items each weighing 5pounds.
    
6.  Click **Save**. Verify that the order is not saved because the weight of the order (10 x 5 lbs) is greater than the maximum weight allowed (25 as set in the script parameter). An error is displayed: 'The total weight of the order is 50. The max weight is 25. Please adjust the order and submit a separate order if necessary' (which is text set in the script).
    
7.  Edit the order so that the **Item Weight** is 1. This will make the total order weight 10 lbs.
    
8.  Click **Save**. Verify that the order is successfully saved.
    

### Related Topics

-   [SuiteCloud Customization Tutorials](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/article_159163143019.html)
-   [Add Custom Button to Run a Suitelet](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157169557654.html)
-   [Calculate Commission on Sales Orders](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157227646073.html)
-   [Copy a Value to the Item Column](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157365998080.html)
-   [Disable Tax Fields](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157174758775.html)
-   [Hide a Column in a Sublist](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157305280561.html)
-   [Populate Item Substitution](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_163631599694.html)
-   [Set a Default Posting Period in a Custom Field](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157297685416.html)
-   [Set Purchase Order Exchange Rate](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157176640777.html)
-   [Set the Item Amount to Zero for Marketing Orders](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157243821278.html)
-   [Set Default Values in a Sublist](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157375587585.html)
-   [Track Customer Deposit Balances](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_157141180023.html)

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
