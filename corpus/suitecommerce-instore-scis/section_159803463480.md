---
id: "section_159803463480"
type: "section"
title: "Avoiding Storage Issues by Using a Fallback Catalog Limit"
branch: "suitecommerce-instore-scis"
category: "commerce"
breadcrumb: "Commerce > SuiteCommerce InStore (SCIS) > SCIS Administrator Guide > SCIS Fallback > Avoiding Storage Issues by Using a Fallback Catalog Limit"
parent: "chapter_1511997390"
source: "https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_159803463480.html"
anchors: ["subsect_159908487470", "subsect_159908489942", "subsect_159906478896", "subsect_159906520303", "subsect_159906540374", "subsect_159804577793", "subsect_159908051540"]
sha256: "8d12b22972352d2dcf609357670ef9d585712c273cd58957ce2fbf8a32abf0c3"
---

Fallback relies on a downloaded copy of your item and price catalog. This copy is refreshed each time a sales associate logs in to the device.

However, mobile iOS and Windows devices have storage limits for a Fallback catalog. Exceeding the storage limit can cause the SCIS application to fail on a device. This limit often applies to catalogs with more than 20,000 items. See [Fallback and Storage Limits for Mobile Apps](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/subsect_157894549839.html) for more information about storage limits.

The Fallback Catalog Limit feature limits periodic catalog downloads to your mobile devices to 20,000 item SKUs. You can select the items to include plus rely on a NetSuite script to flag and include items purchased in recent orders. Manually-selected items have priority over items flagged due to being recently purchased.

## Initial Fallback Catalog Limit Download {#subsect_159908487470}

The first time the script runs, it reviews past orders for items that weren't manually selected for inclusion. It flags and adds those unique items to the manual items until the item count reaches 20,000. These items are included in your initial Fallback item catalog.

## Subsequent Fallback Catalog Limit Downloads {#subsect_159908489942}

The next weekly run looks for items that were sold but not currently in the catalog list. Then the script removes items with the oldest transaction dates and replaces them with items from recent purchases. It also adds items recently marked to **Include Item in Fallback** and removes items that were inactivated. To maintain performance, subsequent catalog downloads consist of catalog updates only.

Tip:

Enabling the Catalog Limit also enables using the Fallback Open Item for SKUs not downloaded. Together, these options ensure that your sales associates are covered in the event of an outage. See [Avoiding Storage Issues by Using the Fallback Open Item](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/section_159111742646.html).

Note:

Changes made to manually include or stop including items can take up to two hours to download.

## Setting up a Fallback Catalog Limit and Open Item option {#subsect_159906478896}

Use these procedures to enable a Fallback Catalog limit and configure how SCIS selects the items to include.

-   [1\. Enable the Fallback Catalog Limit Feature](#subsect_159906520303)
    
-   [2\. Add Fallback Catalog Limit Field Sets](#subsect_159906540374)
    
-   [3\. (Optional) Choose Specific Items to Include](#subsect_159804577793)
    

To adjust the scheduled query used to keep your Fallback Catalog current, see [Working with the Fallback Catalog Limit Script Schedule](#subsect_159908051540).

Important:

Fallback must be enabled in your SCIS setup. To verify that Fallback is ready, go to Commerce > Instore > Fallback Mode Settings. If one or more Subsidiaries or Locations are highlighted, then Fallback is enabled.

### 1\. Enable the Fallback Catalog Limit Feature {#subsect_159906520303}

In addition to enabling a Fallback Catalog Limit, these steps enable the Open Item option.

Important:

If you've used Fallback during internet interruptions and haven't experienced issues, you probably don't need to enable this feature.

#### To enable the Fallback Catalog Limit:

1.  Go to Commerce > InStore > Settings > Search.
    
2.  Click **Edit** for your settings record.
    
3.  Open the **General** subtab.
    
4.  Check the **Enable Fallback Catalog Limit** box.
    
5.  Click **Save**.
    

### 2\. Add Fallback Catalog Limit Field Sets {#subsect_159906540374}

These field sets add the catalog limit check boxes to the item record page.

#### To add Fallback Catalog Limit field sets to your website:

1.  Go to _Commerce > Websites_.
    
2.  Click **Edit** for the website associated with your SCIS setup.
    
3.  Open the **Field Sets** subtab.
    
4.  Locate the **SCIS Fallback** section.
    
5.  Open an insertion field by clicking in the right-hand column of that section.
    
6.  Add the field sets from the **Field Set** list:
    
    1.  Click the list icon.
        
    2.  In the **\- Select -** field, begin entering the first field set:
        
        `Include item in Fallback (Custom)`
        
        (You can also scroll through the list.)
        
    3.  Select the set and click **Add**.
        
    4.  Begin entering the second field set:
        
        `Scis Include item in fallback (Custom)`
        
    5.  Select the set and click **Add**.
        
    6.  Click **Submit**.
        
7.  Click **OK**.
    
    ![Fallback Catalog Limit field set 7](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/img/Commerce/SCIS/SCIS_Fallback_catalog_limit_field_set_7.png)
    
    You'll see the two sets added to the **SCIS Fallback** section.
    
8.  Click **Save**.
    
    Note:
    
    If you disable the **Fallback Catalog Limit** option later, it's not necessary to remove these field sets.
    

### 3\. (Optional) Choose Specific Items to Include {#subsect_159804577793}

In addition to using the Fallback Catalog Limit script to identify items from recent orders, you can choose which items should always be available in Fallback. Items selected manually using these steps take priority over items identified from the transaction history. These manual items aren't replaced when the history changes.

#### To choose items for your Fallback Catalog Limit:

1.  In NetSuite, go to _Lists > Accounting > Items_.
    
2.  Expand the **Filters** list and select **Inventory Item**.
    
3.  Locate the item you want and click **Edit**.
    
4.  Open the **Custom** subtab.
    
5.  Check the **Include Item in Fallback** box.
    
6.  Click **Save**.
    
7.  Repeat for the next item as appropriate.
    

### Working with the Fallback Catalog Limit Script Schedule {#subsect_159908051540}

You can adjust when the script reviews orders and items for inclusion in the Fallback Catalog download. You might make this change to ensure orders from your busiest days are included in the count sooner. The default runtime is weekly on Monday night.

#### To adjust the Fallback Catalog Limit script schedule:

1.  In NetSuite, go to _Customization > Scripting > Scripts_.
    
2.  Expand the **Filters** list and select **Map/Reduce**.
    
3.  Find the **Check fallback items** script and click **View**.
    
4.  Open the **Deployments** subtab and click the script **Title**.
    
5.  To adjust the **Schedule**, click **Edit** and change as you want.
    
6.  Open the **Parameters** subtab.
    
7.  The **Total Items** value is the number of items to be included in your Fallback Catalog Limit.
    
    Warning:
    
    You shouldn't increase the **Total Items** to greater than 20,000. Otherwise, your devices can experience storage issues.
    
8.  Choose the period to **Prioritize Items Sold In**.
    
    This field optimizes how NetSuite populates your Fallback Catalog. The script reviews the transaction history and identifies items that are likely to be purchased. Items are identified by how many times they were sold during the previous period.
    
    The period length should be based on your business volume. If the number of items regularly sold is large, you can limit the number of unique items by reducing the period length. For example, your catalog might be more current or focused if you limit transaction history to the previous quarter or month instead of the previous year.
    
    If the number of items identified as frequent purchases exceeds the **Total Items** limit, NetSuite only includes the items found before the limit was reached.
    
    Note:
    
    Inventory item records with **Include Item in Fallback** checked are added to the Fallback Catalog first. Items found by the script are added next.
    
9.  Click **Save**.
    

[General Notices](https://docs.oracle.com/en/cloud/saas/netsuite/ns-online-help/chapter_N000004.html)
